# Local development default
# For local Hardhat node you do not need to fill these.
# When using hardhat node, the scripts default to http://127.0.0.1:8545 and use the Hardhat dev keys.

# Optional (only if you later deploy to a public testnet)
RPC_URL=                 # e.g. https://eth-sepolia.g.alchemy.com/v2/<KEY>
PRIVATE_KEY=             # 0x... (test wallet private key) - NEVER commit
WEB3_STORAGE_TOKEN=      # optional web3.storage token if you wish to pin to IPFS

# Notes:
# - For local development run: npx hardhat node
# - Use one of the printed Hardhat dev private keys for PRIVATE_KEY when interacting locally
